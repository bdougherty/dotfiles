- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.config/youtube-dl:
      create: true
      path: config/youtube-dl
    ~/.dotfiles: ''
    ~/.bash:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.curlrc:
    ~/.editorconfig:
    ~/.gitattributes:
    ~/.gitconfig:
    ~/.hyper.js:
    ~/.npmrc:
    ~/.shell:
    ~/.vim:
    ~/.vimrc:
    ~/.zsh:
    ~/.zshrc:

- brewfile:
    file: Brewfile
