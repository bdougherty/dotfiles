- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.bash:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.curlrc:
    ~/.editorconfig:
    ~/.gitconfig:
    ~/.hyper.js:
    ~/.npmrc:
    ~/.shell:
	~/.vim:
	~/.vimrc:
    ~/.zsh:
    ~/.zshrc:

- brewfile:
    file: Brewfile
