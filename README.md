# Brad’s dotfiles

## Installation

```bash
git clone git://github.com/bdougherty/dotfiles.git ~/.dotfiles && cd ~/.dotfiles && source install.sh
```

## Updating

```bash
cd ~/.dotfiles
git pull
```
